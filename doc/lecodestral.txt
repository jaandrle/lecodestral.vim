*lecodestral.txt*  Inline ghost-text autocomplete for Vim 9 via Mistral Codestral

Author:  Florian Michel (fmflurry)
License: MIT

==============================================================================
CONTENTS                                                  *lecodestral-contents*

  1. Introduction ........................... |lecodestral-introduction|
  2. Requirements ........................... |lecodestral-requirements|
  3. Setup .................................. |lecodestral-setup|
  4. Usage .................................. |lecodestral-usage|
  5. Commands ............................... |lecodestral-commands|
  6. Mappings ............................... |lecodestral-mappings|
  7. Configuration .......................... |lecodestral-config|
  8. Highlighting ........................... |lecodestral-highlight|
  9. Limitations ............................ |lecodestral-limitations|

==============================================================================
1. INTRODUCTION                                       *lecodestral-introduction*

lecodestral.vim provides Copilot-style inline ghost-text autocomplete for
classic Vim 9, powered by Mistral Codestral through its fill-in-the-middle
(FIM) endpoint. It uses native virtual text (|textprop|) and async jobs
(|job|); it needs no Neovim and no Node, only `curl`.

==============================================================================
2. REQUIREMENTS                                       *lecodestral-requirements*

- Vim 9.0.0067+ with |+textprop| and |+job|
  Check with: >
      :echo has('patch-9.0.0067') && has('textprop') && has('job')
<
- `curl` on $PATH
- A Codestral API key (free tier at https://console.mistral.ai)

==============================================================================
3. SETUP                                                     *lecodestral-setup*

Export your API key in the shell that launches Vim: >

      export CODESTRAL_API_KEY="your-key-here"
<
The variable name is configurable, see |g:lecodestral_api_key_env|.

==============================================================================
4. USAGE                                                     *lecodestral-usage*

Type in insert mode. After a short debounce a grey suggestion appears after
the cursor. Press <Tab> to accept it, or <C-]> to dismiss. When no suggestion
is visible, <Tab> behaves normally (real tab, or popup-menu navigation).

==============================================================================
5. COMMANDS                                               *lecodestral-commands*

                                                              *:LeCodestralToggle*
:LeCodestralToggle      Enable or disable suggestions for the session.

                                                         *:LeCodestralToggleBuffer*
:LeCodestralToggleBuffer      Enable or disable suggestions for the current buffer.

                                                             *:LeCodestralDismiss*
:LeCodestralDismiss     Clear the currently displayed suggestion.

                                                               *:LeCodestralStatus*
:LeCodestralStatus      Show why suggestions are disabled (or that they're
                        enabled) via a popup.

==============================================================================
6. MAPPINGS                                              *lecodestral-mappings*

The following <Plug> mappings are available for custom keybindings:

                                                    *<Plug>(lecodestral-complete)*
<Plug>(lecodestral-complete)
                       Trigger a new completion request.

                                                    *<Plug>(lecodestral-accept)*
<Plug>(lecodestral-accept)
                       Accept the currently displayed suggestion.

                                                    *<Plug>(lecodestral-dismiss)*
<Plug>(lecodestral-dismiss)
                       Clear the currently displayed suggestion.

                                                    *<Plug>(lecodestral-cycle-suggestions)*
<Plug>(lecodestral-cycle-suggestions)
                       Cycle to the next suggestion.

                                                    *<Plug>(lecodestral-cycle-suggestions-prev)*
<Plug>(lecodestral-cycle-suggestions-prev)
                       Cycle to the previous suggestion.

                                                    *<Plug>(lecodestral-cycle-context)*
<Plug>(lecodestral-cycle-context)
                       Cycle through context options: preview, full, line, word..

Example: to map <C-Space> to accept suggestions:>
    inoremap <silent> <C-Space> <Plug>(lecodestral-accept)
<
==============================================================================

==============================================================================
8. FUNCTIONS                                             *lecodestral-functions*

These functions can be called directly for advanced usage.

                                                    *lecodestral#toggle()*
lecodestral#toggle()     Toggle the plugin on/off. Same as :LeCodestralToggle.

                                                    *lecodestral#toggle_buffer()*
lecodestral#toggle_buffer()     Toggle the plugin on/off for the current buffer. Same as :LeCodestralToggleBuffer.

                                                    *lecodestral#dismiss()*
lecodestral#dismiss()    Clear the currently displayed suggestion. Same as
                       :LeCodestralDismiss.

                                                    *lecodestral#accept()*
lecodestral#accept()     Accept the currently displayed suggestion.

                                                    *lecodestral#complete()*
lecodestral#complete()   Trigger a new completion request.

                                                    *lecodestral#on_change()*
lecodestral#on_change()  Internal function called on cursor movement to handle
                       partial acceptance of suggestions.

                                                    *lecodestral#cycle({offset})*
lecodestral#cycle({offset})
                       Cycle through available suggestions. Use positive offset
                       to go forward, negative to go backward. Example:>
                           :call lecodestral#cycle(1)
                           :call lecodestral#cycle(-1)
<
                                                    *lecodestral#cycle_context()*
lecodestral#cycle_context()     Cycle through context options: preview (shows first N lines), full (shows entire suggestion), line (shows first line only), word (shows first word only).

*lecodestral#statusline()*
lecodestral#statusline()    Return a short status string for use in &statusline.
                        Add it to your statusline with: >
                            set statusline+=%{lecodestral#statusline()}
<                       It returns "OFF" when disabled, " ON" outside insert
                        mode, " * " while a request is in flight, "N/M" while
                        cycling through suggestions, and " 0 " otherwise.

*lecodestral#status()*
lecodestral#status()    Show why suggestions are disabled (or that they're
                        enabled) via a popup. Same as :LeCodestralStatus.

==============================================================================
7. CONFIGURATION                                            *lecodestral-config*

Set these before the plugin loads (e.g. in your vimrc).

                                                          *g:lecodestral_enabled*
g:lecodestral_enabled        (default v:true)
        Start with suggestions enabled.

                                                *g:lecodestral_disabled_filetypes*
g:lecodestral_disabled_filetypes (default [])
        List of filetypes for which suggestions are disabled.

                                                            *g:lecodestral_model*
g:lecodestral_model          (default "codestral-latest")

                                                      *g:lecodestral_debounce_ms*
g:lecodestral_debounce_ms    (default 150)
        Idle time in ms after a keystroke before a request fires.

                                                       *g:lecodestral_max_tokens*
g:lecodestral_max_tokens     (default 256)

                                                        *g:lecodestral_max_lines*
g:lecodestral_max_lines      (default 3)
        Maximum number of lines shown in a single ghost-text suggestion.
        The model often keeps generating well past the useful completion,
        and those trailing lines tend to hallucinate; capping the suggestion
        to the first few lines keeps quality high. Set to 0 to disable the
        cap and show the full response.

                                                       *g:lecodestral_max_prefix*
g:lecodestral_max_prefix     (default 8000)
        Max bytes of pre-cursor context sent as the FIM prompt. When the
        prefix is trimmed, it is realigned to the next line boundary so the
        prompt never begins mid-line.

                                                       *g:lecodestral_max_suffix*
g:lecodestral_max_suffix     (default 4000)
        Max bytes of post-cursor context sent as the FIM suffix. When the
        suffix is trimmed, it is realigned to the previous line boundary so
        the suffix never ends mid-line.

                                                      *g:lecodestral_temperature*
g:lecodestral_temperature    (default 0.2)

                                                             *g:lecodestral_stop*
g:lecodestral_stop           (default ["\n\n\n"])
        List of stop sequences sent to the FIM endpoint. Guards against
        runaway multi-block completions. Set to [] to disable.

                                                       *g:lecodestral_accept_key*
g:lecodestral_accept_key     (default "<Tab>")
        Insert-mode key mapped to accept a suggestion.

                                                      *g:lecodestral_api_key_env*
g:lecodestral_api_key_env    (default "CODESTRAL_API_KEY")
        Name of the environment variable holding the API key.

                                                         *g:lecodestral_endpoint*
g:lecodestral_endpoint
        (default "https://api.mistral.ai/v1/fim/completions")
        The API endpoint URL for Codestral FIM completions.

                                                        *g:lecodestral_choices*
g:lecodestral_choices      (default 3)
        Number of completion choices to request from the API. The plugin
        cycles through these choices when using |lecodestral#cycle()|.

                                                        *g:lecodestral_debug*
g:lecodestral_debug        (default 0)
        Enable debug logging to /tmp/lecodestral.log. Set to 1 to enable.

==============================================================================
7. HIGHLIGHTING                                          *lecodestral-highlight*

                                                            *hl-LeCodestralGhost*
Ghost text uses the LeCodestralGhost highlight group, linked to Comment by
default. Override it: >

      highlight LeCodestralGhost ctermfg=245 guifg=#6c7086
<
==============================================================================
8. LIMITATIONS                                         *lecodestral-limitations*

- Free Codestral keys are rate-limited.
- No streaming; one request per debounce window.
- Suggestions only appear in insert mode.

==============================================================================
vim:tw=78:ts=8:ft=help:norl:
